-----frågor till lärare------
skriva:
"SELECT ID FROM artist WHERE name=?";
"SELECT votes FROM bidrag WHERE ID=?";
enklare sätt??
(SELECT a.ID, b.votes
FROM artist a
JOIN bidrag b ON a.ID = b.ID
WHERE a.name = ?;
????)

måste användaren trycka på "acceptera cookies/localstorage" för att kunna rösta (måste det finnas en "accept cookies/localstorage" knapp)?


-----frågor till varandra------